
upd tr c cl


cambiar x shift según timeframe (flechitas), fix navigation callbacks
a warning of conversion when pending open? check for open too

settings con ruedita
settings en un solo dict en session state, que cambia en el settings dialog
add graph extra space as a setting
add settings box: checkbox for confirmation dialog of market b/s, normalization default y range, default +- tpsl
show sltp en el gráfico checkbox
callbacks: SLTPENTRY, alert checkbox, alert level: soft graph (just a s state); graph navigation: soft graph? maybe it works auto
deslizador de ancho de gráfico en los settings
show order types or not in settings (graph lines and data table)
add extra button to TP PL to auto set +-20 (changeable in a number input)
line priority?? a set of values maybe? do not update the dfs, just avoid rendering the labels. passing over the set can work
change alerts checkbox to bottom like custom y
move hide trades checkbox? design wise

graph speedup via saving layers in session state
order history in graph
add 'zoom' square in the graph?

git ignore
symbol data defaults cleanup
file cleanup + imports
cambiar path mt5

colores open n closed market
trade limit if losses (in data etc)
add setting or constant to disable account balance show
tinker with chart graphic and spacing constants
alert sounds
colores (markdown RR string y table data) test cond trades lines
weekly and infinite display of data table

REFACTOR GRAPH WITHOUT CHANGING THE INNER LOGIC: USE THE GRAPH RANGE CLASS
if too slow, refactor graph (load all from backmost timestamp to now, reload on.. change of scale? change of timeframe?
set polling interval to 0.5 or less?

local h/l
implementación trading range según la nota

trailing stops

weekend bugs?







