
bots + manage el otro
upd tr c cl


check graph and table calls, something is slow

load and save settings to a file
clean settings file and display/design of dialog

add wrap to every button (widgets file already done)
remove checkboxes tooltips

line priority?? a set of values maybe? do not update the dfs, just avoid rendering the labels. passing over the set can work
change alerts checkbox to bottom like custom y, and remove callback to disable the other checkbox?

graph speedup via saving layers in session state
order history in graph
add 'zoom' square in the graph?

git ignore
symbol data defaults cleanup
file cleanup + imports
cambiar path mt5

tinker with chart graphic and spacing constants
hourly lines
colores open n closed market. maybe add an is market open row to live data
setting to show hourly lines and open/closed market colors
trade limit if losses (in data etc)
alert sounds
colores (markdown RR string y table data) test cond trades lines
weekly and infinite display of data table

after goto, make custom y range start as true if force default setting is set
also remove goto as default, just assign them as defaults in a constant. clean up goto and first run call in frontend
remove extra shift in the process of refactoring and ''cambiar x shift según timeframe (flechitas), fix navigation callbacks''
REFACTOR GRAPH WITHOUT CHANGING THE INNER LOGIC: USE THE GRAPH RANGE CLASS
if too slow, refactor graph (load all from backmost timestamp to now, reload on.. change of scale? change of timeframe?
set polling interval to 0.5 or less?
cache extra polls for a certain time? all mt5 calls on the project outside of get live data

local h/l
implementación trading range según la nota
trailing stops

weekend bugs?

setting to show hidden trades only if it matters after testing





